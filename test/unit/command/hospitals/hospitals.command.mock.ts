import { signAndSend } from "../../@polkadot-api.mock";

export const hospitals = {
  registerHospital(param) { // eslint-disable-line
    return signAndSend;
  },
  updateHospital(param) { // eslint-disable-line
    return signAndSend;
  },
  deregisterHospital() { // eslint-disable-line
    return signAndSend;
  },
}