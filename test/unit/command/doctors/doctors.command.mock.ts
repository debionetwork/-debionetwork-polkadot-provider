import { signAndSend } from "../../@polkadot-api.mock";

export const doctors = {
  registerDoctor(param) { // eslint-disable-line
    return signAndSend;
  },
  updateDoctor(param) { // eslint-disable-line
    return signAndSend;
  },
  deregisterDoctor() { // eslint-disable-line
    return signAndSend;
  },
}