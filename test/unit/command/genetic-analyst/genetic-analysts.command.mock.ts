import { signAndSend } from "../../@polkadot-api.mock";

export const geneticAnalysts = {
  registerGeneticAnalyst(params) { // eslint-disable-line
    return signAndSend;
  },
  updateGeneticAnalyst(params) { // eslint-disable-line
    return signAndSend;
  },
  deregisterGeneticAnalyst() {
    return signAndSend;
  },
  updateGeneticAnalystVerificationStatus(param1, param2) { // eslint-disable-line
    return signAndSend;
  },
  updateGeneticAnalystAvailabilityStatus(param1, param2) { // eslint-disable-line
    return signAndSend;
  },
  stakeGeneticAnalyst() {
    return signAndSend;
  },
  unstakeGeneticAnalyst() {
    return signAndSend;
  },
  updateMinimumStakeAmount(params) { // eslint-disable-line
    return signAndSend;
  },
  retrieveUnstakeAmount(params) { // eslint-disable-line
    return signAndSend;
  },
  updateAdminKey(params) { // eslint-disable-line
    return signAndSend;
  },
  sudoUpdateAdminKey(params) { // eslint-disable-line
    return signAndSend;
  }
}