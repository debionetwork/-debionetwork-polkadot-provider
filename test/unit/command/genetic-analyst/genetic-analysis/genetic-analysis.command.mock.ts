import { signAndSend } from "../../../@polkadot-api.mock";

export const geneticAnalysis = {
  rejectGeneticAnalysis(param1, param2, param3) { // eslint-disable-line
    return signAndSend;
  },
  processGeneticAnalysis(param1, param2) { // eslint-disable-line
    return signAndSend;
  },
  submitGeneticAnalysis(param1, param2, param3) { // eslint-disable-line
    return signAndSend;
  }
}