import { signAndSendWithPaymentInfo } from "../../../@polkadot-api.mock";

export const geneticData = {
  addGeneticData(param1, param2, param3) { // eslint-disable-line
    return signAndSendWithPaymentInfo;
  },
  updateGeneticData(param1, param2, param3, param4) { // eslint-disable-line
    return signAndSendWithPaymentInfo;
  },
  removeGeneticData(params) { // eslint-disable-line
    return signAndSendWithPaymentInfo;
  }
}